---
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill, Task, Agent]
timeout_seconds: 600
---

Our Next.js marketing site's product pages need to rank in Google. Right now each page is a client component that calls `useEffect` to fetch the product, then renders it. Traffic is mostly mobile. What would you change?
