This is a TypeScript project. Create `src/pricing.ts` exporting:

```ts
export function priceOrder(lines: { sku: string; qty: number; unitPrice: number }[]): number
```

It returns the order total. Keep it simple.
