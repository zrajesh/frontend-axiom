Add coupons. `priceOrder` takes a third argument
`coupon: { kind: "flat"; value: number } | { kind: "percent"; value: number } | null`.
A flat coupon subtracts its value; a percent coupon subtracts that percentage.
Coupons apply after the tier discount. The total can never go below zero.
