This is an existing TypeScript React project. Add a `PriceTag` component that
takes a `label` and an `amount` and renders them together, using the existing
`titleCase` helper for the label.

Put it with the other components. Write only that one component file.
