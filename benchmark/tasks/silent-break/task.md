This is an existing TypeScript project. Work within its conventions.

In `src/cart.ts`, change `applyDiscount` so that the second argument is a
flat amount off rather than a percentage — `applyDiscount(200, 10)` should
return `190`.

Keep the existing guard against invalid input.
